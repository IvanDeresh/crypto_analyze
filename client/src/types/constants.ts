export interface IOurProducts {
  id: number;
  name: string;
  icon: string;
  category: string;
}

export interface IFeedBack {
  userId: number;
  userName: string;
  rating: number;
  text: string;
}
